{
  "name": "step-viewer-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Dev/test tooling only. The site itself is zero-build: index.html loads three + occt-import-js from a CDN via importmap and nothing here is bundled or shipped.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "occt-import-js": "0.0.23",
    "three": "0.160.0"
  }
}
